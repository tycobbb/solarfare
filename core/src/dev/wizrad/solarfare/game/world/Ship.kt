package dev.wizrad.solarfare.game.world

import dev.wizrad.solarfare.game.world.core.Entity
import dev.wizrad.solarfare.generation.ShipNode

class Ship(
  node: ShipNode): Entity<ShipNode>(node) {

}
